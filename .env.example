# Frontend runtime
VITE_GOOGLE_MAPS_API_KEY=
VITE_GEMINI_API_KEY=

# Vercel KV (Redis) — for trip sharing
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Parser runtime hardening
XHS_PARSE_TIMEOUT_MS=30000
XHS_IMAGE_LIMIT=30
XHS_RATE_LIMIT_PER_MINUTE=30
XHS_LOG_REDACT_URLS=true
XHS_COOKIE=
